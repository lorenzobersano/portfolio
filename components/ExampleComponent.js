const ExampleComponent = (props) => (
  <div className="flex items-center justify-center w-full h-32 font-bold text-white bg-red-700">
    This is an example component
  </div>
)

export default ExampleComponent
